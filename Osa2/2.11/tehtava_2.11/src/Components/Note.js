const Note = ({ note }) => {
    
    //{notes.map(x => 
      //  <li key={x.id}> {x.content} </li>
        //)}
    return (
        <div>
        <li> {note.content} </li>
            </div>
    )
}
export default Note