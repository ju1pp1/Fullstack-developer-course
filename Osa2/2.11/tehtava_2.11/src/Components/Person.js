const Person = ({ person }) => {
    
    //{notes.map(x => 
      //  <li key={x.id}> {x.content} </li>
        //)}
    return (
        
        <p> {person.name} </p>
    )
}
export default Person